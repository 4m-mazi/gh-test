name: test
on:
  push:
    branches: ["**"]
  workflow_dispatch:
    inputs: # null for another event
      skip:
        type: boolean
        default: false
permissions: {}

jobs:
  test1:
    if: >
      true
    runs-on: ubuntu-latest
    steps:
      - run: |
  test2:
    if: >-
      true
    runs-on: ubuntu-latest
    steps:
      - run: ''
